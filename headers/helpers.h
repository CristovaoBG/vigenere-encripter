#ifndef __helpers__
#define __helpers__
#include <stdio.h>
#include <stdlib.h>

char pegachar();	 // ajuste do getchara
int pegastr(char *); // ajuste do scanf
void copiastr(char *, char *); //TODO trocar para strcpy
char somachar(char a, char b, char op);

#endif //# __helpers__